{
	"name": "@fangdash/party",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "partykit dev --var API_URL=http://localhost:8787",
		"deploy": "partykit deploy",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@fangdash/shared": "workspace:*",
		"partysocket": "^1.2.0"
	},
	"devDependencies": {
		"partykit": "^0.0.115",
		"typescript": "^6.0.3",
		"vitest": "^4.1.8"
	}
}
